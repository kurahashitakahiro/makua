<div class="chat-body">
<div class="container">
	<div class="row">
		<div class="col-8 offset-2 chat_room">
			<% @entries.each do |entry| %>
				<% current_user == entry.user ? @class = "current_user" : @class = ""  %>
				<% if entry.user != current_user %>
				  <h5 class="text-left <%= @class %>" id="<%= @class %>"  data-id="<%= entry.user.id %>">
				  	<%= link_to "#{entry.user.name}", users_user_path(entry.user_id) ,style: "color:black;"%>'s room</h5>
				<% else %>
				  <h5 class="text-left <%= @class %>" id="<%= @class %>" data-id="<%= entry.user.id %>"></h5>
				<% end %>
			<% end %>
			<div class="chat-border"></div>
			<div id="messages" data-room_id="<%= @room.id %>">
			  <%= render @messages %>
			</div>
			<div class="chat-border"></div>
			<form>
			  <label>New message</label><br>
			  <input type="text" id="chat-input" data-behavior="room_speaker" class="form-control mb-3">
			</form>
		</div>
	</div>
</div>
</div>